name: 'banking_dbt'
version: '1.0.0'
config-version: 2

profile: 'banking_dbt'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  banking_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +schema: marts